name: Apply SPIF config shim

on:
  push:
    branches: [main]
    paths:
      - scripts/apply-spif-config-shim.py
      - .github/workflows/apply-spif-config-shim.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: dtolnay/rust-toolchain@stable
      - name: Apply exact SPIF ROM shim
        run: |
          python scripts/apply-spif-config-shim.py
          cargo fmt --check || cargo fmt
          cargo test
      - name: Commit generated source change
        run: |
          if git diff --quiet; then
            echo "No source change required"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/discovery.rs
          git commit -m "Model PHY6252 spif_config bootstrap ABI"
          git push
