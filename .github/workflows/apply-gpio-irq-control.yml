name: Apply GPIO IRQ control

on:
  push:
    branches: [main]
    paths:
      - scripts/apply-gpio-irq-control.py
      - .github/workflows/apply-gpio-irq-control.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: dtolnay/rust-toolchain@stable
      - name: Apply GPIO IRQ control model
        run: |
          python scripts/apply-gpio-irq-control.py
          cargo fmt --check || cargo fmt
          cargo test
      - name: Commit generated source change
        run: |
          if git diff --quiet; then
            echo "No source change required"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/bus.rs src/discovery.rs
          git commit -m "Model PHY6252 GPIO interrupt enable and mask"
          git push
